# ТЗ: Программа первичной локальной настройки ПЛК ШНО

## 1. Назначение

Desktop-приложение (Avalonia, .NET 10) для локальной первичной настройки ПЛК ШНО по протоколу Modbus TCP.

Разделение ответственности:
- **Сервисный слой (backend)** — данная работа: Modbus-communication, модели данных, профили контроллеров, логика опроса/записи, ViewModels.
- **UI (Views, axaml)** — другой разработчик.

## 2. Подключение и опрос

- Modbus TCP; параметры подключения: **IP**, **Port** (по умолчанию 502), UnitId = 1.
- **Циклический опрос** регистров с настраиваемым периодом, по умолчанию **1000 мс**.
- **Запись приоритетна**: команды записи приостанавливают циклический опрос и выполняются вне очереди опроса (единый потокобезопасный доступ к Modbus-клиенту).
- При обрыве связи — отображение статуса, автоматические попытки переподключения.
- Весь I/O асинхронный, UI не блокируется.

## 3. Профили контроллеров

- Программа поддерживает разные модели ПЛК (EKF, LicOS и др.), отличающиеся **только адресацией**; принцип работы одинаковый.
- Вся карта адресов (базовые адреса блоков, смещения, шаги) выносится в **JSON-профиль**, лежащий рядом с exe.
- Выбор профиля при подключении.
- UI для редактирования профилей — вне текущего scope (планируется позже).

## 4. Функциональные блоки

### 4.1. Дискретные входы (64 шт.)

Каждый вход состоит из:

| Параметр | Тип | Доступ | Примечание |
|---|---|---|---|
| Входное значение | coil | чтение | сырое значение с клеммы |
| Выходное значение | coil | чтение | значение после применения НО/НЗ |
| Селектор входа | holding register | чтение/запись | число 0..64 |
| НО/НЗ | coil | чтение/запись | инверсия входа |
| Наименование | — | — | хранится в приложении (конфиг), не в ПЛК |

Входные/выходные значения — в циклическом опросе.

### 4.2. Пускатели (4 шт.)

- **Регистр управления** — один общий holding register, битовая маска включения (бит 0 = КМ1 … бит 3 = КМ4), запись.
- **Регистр длительности ручного режима** — один общий holding register, целочисленное слово, время работы в секундах; запись 0 = выключить ручной режим.
- **Обратная связь** — состояние пускателей, 4 отдельных coil, чтение в циклическом опросе.

### 4.3. Счётчики (до 6 слотов)

ПЛК сам опрашивает счётчики по своей шине; программа читает/пишет только регистры ПЛК.

Настройка слота:
- **Тип** (holding register): 0 = CE318(CE208), 1 = CC301(CC101).
- **Адрес счётчика** (holding register, одно слово): **0 = слот отключён**, данные этого слота не опрашиваются. Счётчики можно добавлять впоследствии.

Выводимые данные (только для активных слотов, в циклическом опросе):
- Напряжения по фазам — 3 × float
- Токи по фазам — 3 × float
- Мощности по фазам + общая — 4 × float
- Накопленная энергия — float
- Серийный номер — DINT
- Статус связи — bool (coil)

Формат многорегистровых значений: float / DINT = 2 регистра, **big endian**.

Порядок параметров внутри блока слота и шаг между слотами — в JSON-профиле.

### 4.4. Время контроллера

- **Чтение**: 6 регистров (год, месяц, день, часы, минуты, секунды), обычные целые значения; в циклическом опросе.
- **Запись**: отдельные 6 регистров для записи даты/времени + **триггер-coil** «применить».
- Функция синхронизации времени ПЛК с временем ПК.

### 4.5. График работы (CSV)

**Не участвует в циклическом опросе** — только запись из файла и чтение по явному запросу.

#### Формат CSV

- Строка = один день.
- Колонки: `Дата`, затем 8 пар `Время N` / `Режим N` (8 интервалов смены режима в день).
- Пустое время обозначается `--:--`.
- Файл содержит **все 366 дней** (29 февраля учитывается всегда).
- Год в дате информационный, игнорируется.
- Разделитель — стандартный (`;`).

#### Формат слова в ПЛК (1 слово = 1 интервал)

- **Биты 0–11**: время в формате ЧЧММ (например 12:00 → 1200, 13:54 → 1354).
- **Бит 12 = КМ1, бит 13 = КМ2, бит 14 = КМ3, бит 15 = КМ4**.
- **Пропуск** записывается явно: все 12 бит времени = 1 (0xFFF) + маска 0000, т.е. слово `0x0FFF`.

#### Маппинг «Режим» → битовая маска пускателей (КМ1 КМ2 КМ3 КМ4)

| Режим | Маска | Режим | Маска |
|---|---|---|---|
| 0 / 5 | 0000 | 9 | 0110 |
| 1 | 1000 | 10 | 0101 |
| 2 | 0100 | 11 | 0011 |
| 3 | 0010 | 12 | 1110 |
| 4 | 0001 | 13 | 1101 |
| 6 | 1100 | 14 | 0111 |
| 7 | 1010 | 15 | 1111 |
| 8 | 1001 | 16 | 1011 |

Обратный маппинг: маска → режим по той же таблице; **маска 0000 → режим 5** (канонически, для явности).

#### Адресация

- 8 регистров на день.
- Каждый месяц M начинается с адреса `7000 + 248 × (M − 1)` (248 = 31 день × 8 регистров).
- Записываются только **реальные дни** месяца (февраль — всегда 29 дней); «хвосты» до 31 дня **не трогаем**.
- Базовый адрес и шаг — в JSON-профиле.

#### Чтение графика

- Выгрузка в CSV, структурно идентичном входному: те же колонки, даты, времена, `--:--` на пропусках, режимы по обратному маппингу.
- Round-trip канонизируется: после записи и повторного чтения пропуски всегда возвращаются как `--:--` с режимом `5`.

## 5. Нефункциональные требования

- Асинхронный I/O, не блокирующий UI.
- Единый потокобезопасный доступ к Modbus-клиенту; запись приоритетна.
- Локальное сохранение настроек приложения: наименования входов, IP/порт, период опроса, выбранный профиль.
- Логирование ошибок связи (без «пустых» catch).
